# App Name
app_name: biojs-backend

# Deployer User and Groups
deployer_user: root
deployer_group: root

# SSH Keys Directory
ssh_dir: ~/.ssh


# Github Code's Repo URL
code_repository_url: https://github.com/biojs/biojs-backend

# App Directory
app_dir: /root/live/biojs-backend

# DB Configuration
db_url: postgresql://{{deployer_user}}:{{db_password}}@localhost/{{app_name}}
db_password: changeme
db_name: "biojsmain"
db_user: "{{ deployer_user }}"

# Application Dependencies Setup
venv_dir: '/root/live/biojsenv'
venv_python: '{{ venv_dir }}/bin/python'
requirements_file: "{{ app_dir }}/requirements.txt"
# Remote Server Details
server_ip: <Change_Server_IP>
wsgi_server_port: 8000


# Deploy Dir in App Directory
deploy_dir: '{{ app_dir }}/deploy'

# WSGI Vars
django_wsgi_module: biojs.wsgi
django_settings_module: biojs.settings
database_url: '{{ db_url }}'

virtualenv_python_version: python

# Gunicorn Vars

base_project_name: biojs
application_log_dir: "{{ venv_dir }}/logs"
application_log_file: "{{ application_log_dir }}/gunicorn_supervisor.log"
gunicorn_wsgi_module: "{{ base_project_name }}.wsgi"
gunicorn_settings_module: "{{ base_project_name }}.settings"
gunicorn_user: "{{ deployer_user }}"    # Can be changed
gunicorn_group: "{{ deployer_group }}"  # Can be changed

# Nginx settings.
nginx_app_name: biojs_backend
nginx_access_log_file: "{{ application_log_dir }}/nginx_access.log"
nginx_error_log_file: "{{ application_log_dir }}/nginx_error.log"
nginx_static_dir: "{{ app_dir }}/static/"
nginx_media_dir: "{{ app_dir }}/media/"
hostname: "{{ server_ip }}"     # domain name if required
secondary_hostname: ""          # www.hostname in case if hostname is a domain and not an IP

# Database configuration vars

django_project_name: biojs  # Name of base project
GITHUB_CLIENT_ID: GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET: GITHUB_CLIENT_SECRET

# Certbot Variables

nginx_use_letsencrypt: no   # Yes if SSL certificate is required.
nginx_strong_dh_group: no   # Yes recommended with SSL
certbot_auto_renew: yes
certbot_admin_email: admin@example.com  # Email for certificate authentication
certbot_script: /opt/certbot-auto
certbot_output_dir: "/etc/letsencrypt/live/{{ hostname }}"
letsencrypt_dir: "/etc/letsencrypt/live/{{ hostname }}"
letsencrypt_cert_filename: fullchain.pem
letsencrypt_privkey_filename: privkey.pem

# Front End variables

frontend_dir: /root/live/biojs-frontend
frontend_repository_url: https://github.com/biojs/biojs-frontend